-- use Talky

-- CREATE or alter PROCEDURE CreatePost
--     @postID varchar(300),
--     @userID varchar(300),
--     @postPic VARCHAR(255),
--     @content VARCHAR(1000)
-- AS
-- BEGIN
    
--     INSERT INTO Posts (postID, userID, postPic, content, Timestamp)
--     VALUES (@postID, @userID, @postPic, @content, GETDATE())
-- END

-- select * from Posts
